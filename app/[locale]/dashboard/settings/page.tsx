import React from "react";

export const metadata = {
  title: "Settings",
  description: "This is the website's dashboard settings",
};

const Settings = () => {
  return <div>Settings</div>;
};

export default Settings;
